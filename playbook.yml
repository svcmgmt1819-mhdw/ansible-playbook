#!/usr/bin/env ansible-playbook
---

    - name: Basic fresh VM tasks
      hosts: svcmgmt-docker
      roles:
        - apparmor
        - docker
        - login
        - other_packages
        - vbox
        - ansible
        - vagrant